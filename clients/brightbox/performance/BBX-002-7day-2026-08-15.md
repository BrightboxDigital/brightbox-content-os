# BBX-002 — 7-day performance check

Run 2026-08-15. Published 2026-08-03T13:53:08 UTC, so this check is running 12 days post-publication,
later than the intended ~7-day window (nothing in the tracker triggered it earlier; this is the first
scheduled run to cross the 7-day mark for this article).

## Live URL and indexing health

**Finding: the article's URL structure changed since publication.** The URL recorded in the tracker,
`https://brightboxdigital.io/blog/local-services-ads-moving-to-google-ads/`, now returns **301** and
redirects to `https://brightboxdigital.io/local-services-ads-moving-to-google-ads/` (the `/blog/`
segment was dropped). Checked directly with `curl -D -`.

The new destination is healthy:

| Check | Result |
|---|---|
| HTTP status | 200 |
| Robots meta | `follow, index, max-snippet:-1, max-video-preview:-1, max-image-preview:large` |
| Canonical | Self-referencing: `https://brightboxdigital.io/local-services-ads-moving-to-google-ads/` |
| In post-sitemap.xml | Yes, listed at the new URL |
| Article schema | `BlogPosting` present |

So the article itself is not broken and is correctly indexed at its current URL. But two things now
point at the stale pre-redirect URL and pay an unnecessary redirect hop on every click:

- The tracker's `published_url` field (this file's own source of truth).
- The one live inbound link, from `/google-and-facebook-ads/`, which links to
  `/blog/local-services-ads-moving-to-google-ads/` and 301s through to the real URL.

**Also found while checking inbound links:** the tracker's notes state "2026-08-03: Archie added the
two recommended inbound links himself (`/google-and-facebook-ads/` and the BBX-001 article)." Only
the `/google-and-facebook-ads/` link is actually present on the live site. The BBX-001 article
(`does-a-small-google-ads-budget-work`) does not currently link to BBX-002 anywhere on the page,
checked by fetching and searching its full HTML for both the slug and the phrase "Local Services Ads."

Neither of these blocks indexing. Both are worth a few minutes in Elementor: repoint the existing
link to the current URL, and confirm whether the BBX-001 inbound link was meant to be added but
wasn't, or was added and later removed.

**Also still open, already logged:** the blog archive at `/blog/` still does not surface this post
(site-fix-backlog.md #4, first confirmed 2026-08-03 during publish QA, confirmed still true today).

## Search Console and GA4, 7-day window

Window 2026-08-06 to 2026-08-13 (`./scripts/performance-check local-services-ads-moving-to-google-ads --days 7`).

| Metric | This check | Day-0 baseline (2026-08-03) |
|---|---|---|
| GSC impressions | 0 | 0 |
| GSC clicks | 0 | 0 |
| GA4 sessions | 0 | 0 |

No change from the publication-day baseline. Recorded to `performance/baselines.csv`
(`2026-08-15, BBX-002 7-day post-publication check`).

## Diagnosis

Per `.claude/skills/monitor-blog/SKILL.md` and CLAUDE.md: this is too early and too thin a dataset to
judge performance, and the numbers should not be read as a verdict on the article. This is primarily
an indexing and technical-health check, and on that front the article passes: it is live, indexed,
correctly canonicalized, and in the sitemap. The URL structure change and the stale inbound link are
the only real findings, and both are link-hygiene items, not indexing or ranking problems.

**Do not add words or keywords in response to this.** Nothing here calls for that.

## Recommended next action for Archie

1. Confirm the URL migration was intentional (permalink structure change site-wide, or something
   else). If intentional and permanent, update the `/google-and-facebook-ads/` inbound link and the
   tracker's `published_url` to the new URL directly rather than relying on the redirect.
2. Decide whether BBX-001 should link to BBX-002 as originally noted, and add it if so.
3. No action needed on GSC/GA4 numbers yet. Recheck at the 28-day mark (~2026-08-31).
